Project Setup Guide

Follow the steps below to set up and run the project locally.

Step 1: Install Dependencies

Run the following command to install all required packages:

npm i

Step 2: Start the Development Server

After installation, start the development environment using:

npm run dev


Project should now be running locally.
Make sure to check your terminal for the local URL (http://localhost:3000).
